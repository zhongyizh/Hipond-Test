<view catchtap="onMaskTap" catchtouchmove="doNothingMove" class="container-mask {{center?'center':''}} l-class" hidden="{{show?'':'hidden'}}" style="z-index:{{zIndex}}; {{show?'background: rgba(0,0,0,'+opacity+')':''}}">
    <view class="mask-content l-mask-class">
        <slot></slot>
    </view>
</view>
